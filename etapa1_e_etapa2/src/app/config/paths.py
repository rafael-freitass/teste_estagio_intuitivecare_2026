from pathlib import Path

BASE_DATA = Path("data")

TEMP = BASE_DATA / "temp"
OUTPUT = BASE_DATA / "output"

DOWNLOAD = TEMP / "download"
EXTRACTED = TEMP / "extracted"
NORMALIZED = TEMP / "normalized"
PROCESSED = TEMP / "processed"
CONSOLIDADO = TEMP / "consolidado"